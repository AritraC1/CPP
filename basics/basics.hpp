// #pragma once is a header guard. 
// Its job is to make sure a header file is included only once per compilation unit.
#pragma once

// Variables
void variables();

// Conditionals
void isAdult(int age);

// Switch statements
void dayDate(int num);

// Loops
void findEvenNumbers(int num);
void numbers(int num);
void odd(int num);

// STL
void stl();

