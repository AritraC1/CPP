#pragma once
#include <vector>

// Count all digits of a number
int count_all_digits(int n);

// Reverse a number
int reverse_number(int num);

// palindrome
bool check_palindrome(int num);

// GCD (Greatest Common Division)
int gcd(int a, int b);

// Armstrong Number
bool check_armstrong(int num);

// All Divisors of a number
std::vector<int> print_all_divisors(int num);

// Prime number
bool check_prime(int n);

